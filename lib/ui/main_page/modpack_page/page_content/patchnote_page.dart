import 'package:flutter/material.dart';

class PatchnotePage extends StatelessWidget {
  const PatchnotePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
